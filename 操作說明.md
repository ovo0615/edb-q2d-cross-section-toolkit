# EDB to Q2D — 操作說明

在 Ansys EDB 的 2D Layout 上指定一條切線，工具會萃取該處**真實存在**的層截面，
在 Ansys Q2D Extractor 中重建成二維模型並求解特徵阻抗。

---

## 1. 事前準備

### 需要先安裝的東西

| 項目 | 說明 | 取得方式 |
| --- | --- | --- |
| Windows 10／11 64 位元 | 作業系統 | — |
| Python 3.10（64 位元） | 執行環境 | 啟動器會自動偵測；找不到時以 WinGet 於使用者範圍安裝，不會動到既有的 Python |
| Ansys Electronics Desktop | **2026 R1**（預設）或 **2024 R2**，需含 **Q2D Extractor** 授權 | 公司授權 |
| 網路連線 | 僅第一次安裝 Python 套件時需要 | — |

第一次執行時，啟動器會在 `web_app\backend\.venv` 建立隔離環境，
並安裝以下套件（版本已鎖定於 `requirements.lock.txt`）：

| 套件 | 用途 |
| --- | --- |
| `pyedb==0.78.2` | 讀取 EDB 的幾何、疊構、材料、padstack 與網路 |
| `pythonnet==3.1.0` | pyedb 在 Windows 載入 Ansys EDB DLL 所需的 .NET 橋接層 |
| `pyaedt==1.1.0` | 建立並求解 Q2D Extractor 設計 |
| `fastapi==0.141.1` | 提供瀏覽器介面的本機 HTTP API |
| `uvicorn[standard]==0.52.3` | 綁定在 `127.0.0.1` 的 ASGI 伺服器 |

檔案瀏覽對話框使用 Python 標準函式庫的 `tkinter`，
Windows 官方 Python 安裝檔預設已包含，不需另外安裝。

### 輸入檔案

需要一個 `.aedb` **資料夾**（不是檔案）。
若手上只有 SIwave 的 `.siw`，請先在 SIwave 中執行 `File → Export → EDB`。

**沒有板子也可以先試。** 解壓縮後的根目錄有一個示範板 `Example_SYZ.aedb`
（8 個導體層、4608 個圖元、308 條 net），本文所有截圖都是用它拍的，
照著走一次會得到一樣的數字。

---

## 2. 啟動

1. 把整包**完整解壓縮**到本機資料夾。
   不要直接在壓縮檔的瀏覽視窗裡執行——那樣只會解出單一檔案，其餘相依檔案都不存在。
2. 雙擊 `web_app\start.bat`。
3. 第一次執行會花數分鐘建立環境並安裝套件，過程會顯示在啟動器視窗中。
4. 就緒後瀏覽器會自動開啟 `http://127.0.0.1:8010`。
   若因防毒或群組原則無法自動開啟，啟動器會把網址印在視窗中，自行複製貼上即可。

**結束服務**：關閉啟動器視窗，或在視窗中按 `Ctrl+C`。
啟動器會收掉整棵程序樹，並實際確認連接埠 8010 已釋放。

**連接埠被佔用**：啟動器會顯示佔用者的 PID 與程序名稱，不會擅自終止不明程序。
可改用其他連接埠：

```bash
powershell -ExecutionPolicy Bypass -File web_app\start.ps1 -Port 8011
```

---

## 3. 介面配置

```
┌──────────────────────────────────────────────────────────────┐
│  ▨  EDB to Q2D                                    虎門科技標誌 │  標題列
├──────────────────────────────────────────────────────────────┤
│  檔案   執行   檢視   說明                 服務正常 · 本機運算 │  選單列
├───────────────┬──────────────────────────────────────────────┤
│ 1 板檔        │  Layout │ 結構剖面 │ Q2D 模型 │ 結果          │
│ 2 工作範圍    │                                              │
│ 3 截面        │              （檢視區）                       │
│ 4 訊號線      │                                              │
│ 5 求解        ├──────────────────────────────────────────────┤
│               │              系統日誌                         │
└───────────────┴──────────────────────────────────────────────┘
```

**左側面板**是第一次用時照順序走的路；**選單列**是已經知道要做什麼時抄的近路。
兩者指向同一批動作，選單裡沒有任何「只能從選單做」的功能。做不到的項目會變灰，
不會消失——位置固定，才記得住東西在哪裡。

| 選單 | 內容 |
| --- | --- |
| **檔案** | 瀏覽 EDB…（`Ctrl+O`）、開啟板檔、儲存截面設定（`Ctrl+S`）、複製日誌、清除日誌 |
| **執行** | 框選工作範圍、新增切線、刪除目前切線、重新掃描目前截面、建立並求解（`F5`）、終止求解（`Esc`） |
| **檢視** | Layout／結構剖面／Q2D 模型／結果，目前那個打勾 |
| **說明** | 快速上手、關於 EDB to Q2D |

快捷鍵在輸入框裡不會觸發，所以在路徑欄位打字不會誤觸存檔。

![選單列](docs/images/gui-09-menu.png)

分隔線都可以拖曳（左側面板／檢視區、檢視區／日誌），拖過的位置會以**比例**記住，
換一台不同解析度的機器開啟時版面比例維持一致。

---

## 4. 操作流程

### 步驟 1：開啟板檔

1. 「板檔」區按 **瀏覽…**，在對話框中選 `.aedb` 裡的 **`edb.def`**。
   工具會自動折回外層的 `.aedb` 資料夾。
   也可以直接把路徑貼進輸入框——帶引號、或指到 `edb.def` 都會自動修正。
2. 選擇 **AEDT 版本**。預設 `2026.1`。
   選到未驗證的版本時，介面會顯示提示——仍可使用，但結果請自行覆核。
3. 按 **開啟**。

板子載入後，「Layout」分頁會顯示 2D Layout，右側是 SIwave 風格的圖層面板。

![開啟板檔](docs/images/gui-01-layout.png)

**同一片板子存過的截面設定會自動載回來**，包含切線位置、角色指派與上次的求解結果。
載回來的切線只有位置沒有截面，所以工具會自動補掃一次——不必自己按重新掃描。

> **圖元數很多的板子**
> 超過上限時會自動降級為「粗略模式」（多邊形以外框近似），介面上方會出現提示。
> 切線掃描與 Q2D 建模**永遠使用精確幾何**，降級只影響畫面渲染速度，不影響任何數值結果。

### 步驟 2：圖層面板（Layout 分頁）

面板每一列是一個**導體層**，七個欄位由左至右：

| 欄位 | 圖示 | 作用 |
| --- | --- | --- |
| Fill | ■ | 銅箔平面實心填滿／只畫外框 |
| Show | ● | 顯示／隱藏整層 |
| Planes | ▣ | 銅箔平面 |
| Traces | ≡ | 走線 |
| Pads | ◉ | 焊盤 |
| Vias | ◎ | 過孔 |
| Elements | ⊞ | 電路元件 |

**Fill 預設是關閉的。** 銅箔平面實心填滿會蓋住底下的走線、pad 與 via，
而剛載入一片板子時最需要看的正是那些。

面板只列導體層；介質層在 Layout 上沒有任何幾何可以開關，列出來只會多出一半按了
沒反應的核取方塊。**但結構剖面裡仍會列出每一層**，兩份清單服務不同的問題。

**點擊層名**可只顯示該層，再點一次還原全部；欄位標題可整欄開關。
面板邊緣的 **◀** 可把整個面板收起，收起後同一位置會變成 **▶**，按一下展開回來。
切換到 **Nets** 分頁可依網路名稱過濾，找特定差分對時很有用。

**畫面操作**：左鍵拖曳平移，滾輪以游標為中心縮放；右下角 **⛶ Fit All** 回到全板，
**▣ 範圍** 回到目前的工作範圍。游標座標即時顯示在右下角。

### 步驟 3：框選工作範圍

整片 200 mm 的板子上要對準一條 0.1 mm 的走線，不是介面問題，是尺度問題。
所以先縮小範圍，再在裡面裁切。

1. 「工作範圍」區按 **▣ 框選範圍**（或選單 **執行 → 框選工作範圍**）。
2. 在 Layout 上拖出一個矩形，涵蓋你要分析的那一段。
3. 畫面會自動縮過去，範圍外壓暗。

範圍框好之後，介面會顯示它的 X／Y 區間與尺寸。按 **清除** 可解除。

![框選工作範圍](docs/images/gui-02-region.png)

> **工作範圍同時是兩件事**：切線的長度由它決定，而它的左右邊界就是截面的
> **側向截斷邊界**。框太窄會把回流路徑切掉，阻抗偏高；框太寬則模型變大、求解變慢。
> 判斷方式是加大範圍直到數值不再變化。

### 步驟 4：放切線

1. 「截面」區選 **切線方向**：水平（沿 X）或垂直（沿 Y）。
2. 按 **＋ 新增切線**（或選單 **執行 → 新增切線**）。
3. 在工作範圍內**點一下**，切線就放在那裡，兩端自動貼齊工作範圍。

> **切線只能是正 X 或正 Y。**
> 斜切會讓導體寬度看起來變寬（45° 時 +41%），阻抗直接偏低；
> 而滑鼠很難拉出真正水平的線，差 1° 你看不出來，數字卻已經錯了。
> 所以工具不提供斜切，只提供一個座標。

放好後會立刻掃描。**微調**：直接拖曳切線即可，放開後自動重新掃描。
切線清單每一列顯示名稱、座標與 `Zdiff`（未求解時顯示「未求解」）。
按 **刪除** 移除目前選中的那一條。

切線下方會出現這條切線的**求解計畫**：

- **訊號線 N 條**、**參考導體 有／無**
- 若截面上有多條參考網路，會提示它們**在模型中合併為單一導體 `GND`**——
  這假設它們在交流上同電位。要看其中某一條的耦合，就得在「訊號線」把它改回訊號。
- **可算差分 N 對**、**單端 N 條**，以及每一對的正負極性
- 未配對的訊號、以及無法配對的原因

![放切線與求解計畫](docs/images/gui-03-cutline.png)

### 步驟 5：訊號線

這裡列的是**這條切線真正切到的 net**，不是工作範圍裡的所有 net——
列出切不到的只會讓人在無關的項目上做決定。

| 動作 | 說明 |
| --- | --- |
| 核取方塊 | 納入／排除這條 net |
| **設為參考** | 把一條訊號提升為參考平面（例如刻意接地的鄰線） |
| **參考 ✕** | 你提升過的，點一下改回訊號 |
| **參考**（灰標籤） | EDB 標記為 power／ground，鎖定，不可排除 |
| 全部納入／全部排除 | 對目前過濾結果批次操作 |
| 匯入清單／匯出清單 | 純文字檔，一行一個 net。匯出的是**要保留的訊號**，不是被排除的 |

**排除一條 net 的語意是「這塊銅箔不存在」**，該處會變成介電質，
不是把它從結果表裡藏起來。所以排除之後算出來的是拿掉它之後的阻抗，
不是這片板子原本的阻抗——介面會用橘色提示這件事。

**參考導體的判定依據是 EDB 自己的 `is_power_ground` 旗標，不是名字叫不叫 GND。**
沒有參考平面，阻抗根本沒有定義，所以那不該是一個能誤按進去的狀態。

<details>
<summary><b>電源網路被當成參考，是對的還是錯的？</b></summary>

三個問題依序問下去就能判斷：

1. **這片電源銅是不是訊號線最近的參考面？** 是 → 一定要當參考。
   有去耦的電源平面在高頻就是交流地，這是 SI 的標準假設；不當參考反而會得到
   一個沒有回流路徑的錯誤模型。
2. **兩者之間有沒有一片完整的參考平面擋著？** 有 → 設不設都無所謂。
   實測過一個案例：訊號對在 L01、參考面是 50 µm 下方的 L02，電源網路在 L10、
   距離 0.764 mm 且被橫跨整個截面的 L09 擋住。把它整個移除重解，`Zdiff` 從
   61.554 變成 61.554——差異比重跑的網格雜訊（0.001）還小。
3. **電源銅在這個截面上是平面還是窄線？** 若是窄線又靠近訊號，這個標記值得懷疑：
   把一條窄線當成理想回流路徑是偏樂觀的假設。

另外，如果你要看的正是訊號對電源網路的耦合（PDN 雜訊），把它併進 `GND`
等於先假設了答案是零。那種情況要在這裡把它改成訊號。

</details>

### 步驟 6：檢查結構剖面

「結構剖面」分頁是**即將建立的 Q2D 模型**的所見即所得預覽。
這是唯一能在花掉一次求解之前抓到錯誤的地方。

- **所有疊構層一律列出**。切線上沒有導體的訊號層會明確標示留白——
  把空層省略掉正是參考面被誤判的成因。
- **黃色** = 訊號導體，**灰色** = 參考導體，**深藍** = 介電層。
- 外框為橘色的方塊表示該導體來自 **via pad**。
- 右側顯示每層的厚度與材料。
- **點擊任一導體方塊**即可切換訊號／參考。

![結構剖面與訊號線清單](docs/images/gui-04-cross-section.png)

上圖左側同時看得到第 4 步的訊號線清單：`GND` 與 `VCC` 帶著鎖定的「參考」標籤，
其餘五條是訊號；右側的 `L6` 與 `BASE` 標示「此層在切線上沒有導體」。

上方會統計安全性提示的數量，下方列出細節，分兩級：

| 等級 | 意義 | 建議 |
| --- | --- | --- |
| **必須修正** | 幾乎確定會得到錯誤的答案 | 修正後再求解 |
| **需判斷** | 可能是刻意的，也可能是失誤 | 自行確認 |

常見提示：

- **切線未垂直於走線** — 斜切會讓線寬看起來變寬，阻抗因此偏低。
  提示會附上偏差角度與線寬被放大的百分比。
- **切線穿過 via pad** — via pad 比走線寬得多，會把該處的截面撐大。
  刻意要分析 via 位置就忽略；否則把切線移開。
- **沒有參考導體** — 截面裡找不到任何參考層，阻抗無法定義。
- **側向截斷太靠近訊號** — 加大工作範圍直到數值穩定。

### 步驟 7：求解

1. 選 **求解精度**：

   | 模式 | 設定 | 適用時機 |
   | --- | --- | --- |
   | **快速** | 表面阻抗，收斂 1% | 在同一片板上連續試很多條切線。實測比高精度低約 0.4%，16–39 秒 |
   | **標準**（預設） | 表面阻抗，收斂 0.2% | 一般用途。實測比高精度低約 0.2%，22–42 秒 |
   | **高精度** | 在導體內部解場，收斂 0.2% | 要看趨膚效應造成的頻率相依電阻時。107–147 秒 |

2. 確認 **自適應頻率**（預設 `8GHz`）。
3. 按 **建立並求解**（或 `F5`）。

這會啟動 Ansys Electronics Desktop 並**消耗一份授權**。
**AEDT 在背景執行，不會開視窗**；求解過程的日誌即時顯示在底部，
EDB 與 AEDT 都在子程序中執行，瀏覽器介面不會卡住。

**模型一建好、求解還沒開始，畫面就會自動切到「Q2D 模型」分頁。**
預設顯示的是 **AEDT 自己畫的那張截面圖**——AEDT 沒有視窗可看，所以這是
「Q2D 裡真的有什麼」的唯一直接證據。它是等比例的，35 µm 的銅箔在 0.8 mm 的
疊構上只有幾個像素，量不了尺寸；要量層厚與線寬請按 **示意圖** 切過去，
那裡縱向被刻意放大，滑過任一矩形可看它的名稱、材料與尺寸。

![AEDT 建出來的模型](docs/images/gui-05-q2d-model.png)

![示意圖：縱向放大](docs/images/gui-06-q2d-schematic.png)

發現不對就按 **■ 終止求解**（或 `Esc`）：

- AEDT 會在數秒內停下，不必等這一輪跑完。
- **已經完成的截面會保留**，不需要為了改一條切線把前面全部重跑。
- 終止不算失敗，日誌會明確告訴你完成了幾條。

### 步驟 8：讀結果

「結果」分頁上半是選中切線的完整結果：

- **差分**：每一對的 `Zdiff`／`Z_odd`／`Z_even`／`Zcomm`，以及**依據**欄——
  「精確」表示截面上剛好兩個導體；「近似」表示三個以上，此對取 2×2 子區塊、
  其餘導體視為參考電位。
- **單端**：每個導體的 `Z₀`。
- **RLGC 矩陣**：`C`（pF/m）與 `L`（nH/m）。

![結果](docs/images/gui-07-results.png)

矩陣的非對角項就是耦合。下圖這個例子裡，`ST_CLK_CNT4` 與 `ST_DELAY_STROBE`
之間的互容是 −59.8 pF/m、互感 140 nH/m，其餘配對幾乎為零——這兩條走線是鄰居。

![RLGC 矩陣](docs/images/gui-08-rlgc.png)

下半是所有已求解切線的並排比較。這個工具最常見的用法是對照，把數字分散在
不同畫面上，比較就得靠記憶，而記憶會出錯。表格刻意多列兩欄：

| 欄位 | 為什麼要看 |
| --- | --- |
| **導體** | 導體數不同代表截面上的東西不一樣。多一條導體就是多一個耦合對象，而且會讓差分值從精確變成近似（`≈`） |
| **精度** | 求解設定的差別，不是板子的差別 |
| **排除的 net** | 拿掉了哪些銅 |

**兩列的差異只有在這三欄相同時，才代表結構的差異。**

### 步驟 9：存檔

求解完成會自動存成 `<板名>.aedb.q2dcuts.json`（與 `.aedb` 同層）。
也可以隨時用 **檔案 → 儲存截面設定**（`Ctrl+S`）手動存。

存的不只是阻抗，還包含導體清單、求解精度與排除的 net——
兩個數字擺在一起，若不知道各自的前提就無從比較。

> 若 `.aedb` 所在的資料夾沒有寫入權限（例如唯讀的專案目錄），
> 設定會改存到 `%USERPROFILE%\.edb-to-q2d\cutsets\`，介面上會顯示實際存放位置。

下次開啟同一片板子時，切線、角色指派與結果都會自動還原。

---

## 5. 疑難排解

| 症狀 | 原因與處理 |
| --- | --- |
| 啟動器出現 `'indows'`、`'NCODING'` 之類被切斷的指令 | `start.bat` 的換行被轉成 LF。請重新下載，不要用會改寫換行的工具處理。 |
| 瀏覽器沒有自動開啟 | 防毒或群組原則阻擋。啟動器視窗中有網址，自行複製貼上。 |
| 開啟板檔卡很久 | 大型 EDB 的第一次開啟本來就慢（`pyedb` 需載入 .NET 元件）。日誌會顯示進度。 |
| 第 4 步「訊號線」是空的 | 那條切線還沒掃描。選一下它會自動補掃；仍為空就用 **執行 → 重新掃描目前截面**。 |
| 求解失敗，日誌出現授權訊息 | 確認 Q2D Extractor 授權可用，且沒有其他 AEDT 工作階段佔用。 |
| 算出來的阻抗明顯偏低 | 先看安全性提示有沒有「切線未垂直於走線」。斜切會放大線寬。 |
| 算出來的阻抗明顯偏高 | 側向截斷太窄。加大工作範圍直到數值不再變化。 |
| 兩條切線的結果差很多 | 先比對結果表的「導體」「精度」「排除的 net」三欄。前提不同時，數字的差異不代表結構的差異。 |
| 差分值旁邊有 `≈` | 截面上有三個以上的導體，差分是近似值。要精確值就把不相干的導體排除，或確認它們是否該是參考。 |
| 關掉視窗後 AEDT 還在 | 求解中的 AEDT 是獨立程序，工具不會強制終止正在求解的工作階段。請在工作管理員確認。 |

---

## 6. 資料與隱私

- 所有分析都在本機 `127.0.0.1` 執行，**不會上傳任何板檔或結果**。
- 服務只綁定 loopback，區域網路上的其他機器連不到。
- 唯一需要網路的時機是第一次安裝 Python 套件。

---

僅供技術展示，保留所有權利。詳見 [NOTICE.md](NOTICE.md)。
Ansys 為 Ansys, Inc. 之商標，本專案與 Ansys, Inc. 無官方隸屬關係。
